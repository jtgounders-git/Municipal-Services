@model Prog7312PoePart1.Models.ServiceRequest

@{
    ViewData["Title"] = "Create Service Request";
}

<!-- Video Background -->
<div class="video-background">
    <video autoplay muted loop id="bgVideo">
        <source src="~/videos/Cat.mp4" type="video/mp4" />
        Your browser does not support HTML5 video.
    </video>
    <div class="video-overlay"></div>
</div>

<!-- Main content container over the video -->
<div class="container content-over-video py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card card-overlay shadow-sm mb-5">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">Create Service Request</h3>
                </div>
                <div class="card-body">

                    <!-- Encouraging messages for the user -->
                    <div id="encouragementBox" class="alert alert-info text-center mb-4">
                        <span id="encouragementMessage">
                            You're helping improve your community! Every request counts.
                        </span>
                    </div>

                    <!-- Form for submitting a service request -->
                    <form asp-action="Create" enctype="multipart/form-data" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <!-- Title -->
                        <div class="mb-3">
                            <label asp-for="Title" class="form-label fw-semibold">Title</label>
                            <input asp-for="Title" class="form-control" placeholder="Enter a short title" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <!-- Category -->
                        <div class="mb-3">
                            <label asp-for="Category" class="form-label fw-semibold">Category</label>
                            <select asp-for="Category" class="form-select">
                                <option value="">-- Select a Category --</option>
                                <option>Sanitation</option>
                                <option>Roads</option>
                                <option>Utilities</option>
                                <option>Safety</option>
                                <option>Other</option>
                            </select>
                            <span asp-validation-for="Category" class="text-danger"></span>
                        </div>

                        <!-- Location -->
                        <div class="mb-3">
                            <label asp-for="Location" class="form-label fw-semibold">Location</label>
                            <input asp-for="Location" class="form-control" placeholder="Enter the location" />
                            <span asp-validation-for="Location" class="text-danger"></span>
                        </div>

                        <!-- Description -->
                        <div class="mb-3">
                            <label asp-for="Description" class="form-label fw-semibold">Description</label>
                            <textarea asp-for="Description" class="form-control" rows="5" placeholder="Describe the request in detail"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <!-- Status is automatically set to Waiting -->
                        <input type="hidden" asp-for="Status" value="@RequestStatus.Waiting" />

                        <!-- Priority -->
                        <div class="col-md-2">
                            <label asp-for="Priority" class="form-label fw-semibold">Priority</label>
                            <select asp-for="Priority" class="form-select">
                                <option value="1">High</option>
                                <option value="2">Medium</option>
                                <option value="3">Low</option>
                            </select>
                            <span asp-validation-for="Priority" class="text-danger"></span>
                        </div>

                        <!-- Optional media upload -->
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Attach Media/ File</label>
                            <input type="file" name="mediaFile" class="form-control" />
                        </div>

                        <!-- Form buttons -->
                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                            <button type="submit" class="btn btn-primary">Submit Request</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        const messages = [
            "You're helping improve your community! Every request counts.",
            "Small actions make a big difference — thanks for submitting!",
            "Together we can make our town cleaner and safer!",
            "Your voice matters — thank you for speaking up!",
            "Every request is a step toward a better community!"
        ];

        let index = 0;
        setInterval(() => {
            index = (index + 1) % messages.length;
            document.getElementById("encouragementMessage").textContent = messages[index];
        }, 4000);
    </script>
}
